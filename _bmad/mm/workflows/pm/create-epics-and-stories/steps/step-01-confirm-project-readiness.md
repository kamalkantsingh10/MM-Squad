# Step 1: Confirm Project Readiness

## CONTEXT BOUNDARIES:
- Variables from workflow.yaml and step 0 are available in memory
- project_id, group_id, and gitlab_url are already configured in the gitlab-mcp server config
- Do not assume knowledge from other steps — only what was communicated explicitly

## EXECUTION RULES:
- Communicate in {communication_language} with {user_name}

## YOUR TASK:

Note: project_id, group_id, and gitlab_url are already configured in the gitlab-mcp server config — no need to ask.

Confirm with {user_name} that the GitLab project is ready for initialisation.

## COMPLETION:
**Proceed to:** `{installed_path}/steps/step-02-initialise-gitlab-project.md`
